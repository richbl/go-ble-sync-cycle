package ui

import (
	"github.com/richbl/go-ble-sync-cycle/internal/logger"
)

// setupSessionLogSignals wires up event listeners for the logging tab (Page 3)
func (sc *SessionController) setupSessionLogSignals() {

	// Placeholder: Future logging refactor will append messages to sc.UI.Page3.TextView
	// For now, stub with no-op or basic visibility handler if needed
	logger.Debug(logger.GUI, "Session Log: signals setup complete (stubbed)")

}
